server:
  port: 3302

spring:
  application:
    name: cloud-config-client
  profiles:
    active: dev
  cloud:
    config:
      name: cloud-config-client-dev
      uri: http://localhost:3301
      label: main
      profile: dev

management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"

data:
  env: NaN
  user:
    username: NaN
    password: NaN